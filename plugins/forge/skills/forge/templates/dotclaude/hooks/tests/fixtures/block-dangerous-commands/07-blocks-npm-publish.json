{
  "name": "blocks npm publish without dry-run",
  "stdin": {
    "tool_input": {
      "command": "npm publish"
    }
  },
  "env": {
    "PROTECTED_DIRS": "src",
    "PROTECTED_BRANCHES": "main,master"
  },
  "expect_exit": 2,
  "expect_stdout_contains": [
    "registry"
  ]
}