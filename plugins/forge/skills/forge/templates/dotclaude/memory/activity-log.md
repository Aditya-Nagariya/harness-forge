# Activity log

Append-only. One line per completed unit of work: `[YYYY-MM-DD] [who] — what was done — key outcome.` Partially auto-appended by `.claude/hooks/update-status.sh` on health flips.

---

[{{DATE}}] [forge] — Harness installed/upgraded by /forge (version {{FORGE_VERSION}}).
